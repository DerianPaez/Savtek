---
const { className, id, icon, name } = Astro.props
---

<button
  id={id}
  class={`${className} w-max bg-gradient-to-b from-primary via-lightBlue to-secondary text-white py-2 px-4 xl:py-3 xl:px-5 rounded-full flex gap-2`}
>
  {icon && <img src={icon} alt={name} />}
  {name}
</button>
